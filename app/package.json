{
  "name": "electron-app",
  "version": "1.0.0",
  "main": "main.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "start": "set IS_DEV=true && electron .",
    "debug": "set IS_DEV=true && electron --inspect=5858 .",
    "build": "set IS_DEV=false && electron-packager . GIOverlay --platform=win32 --arch=x64"
  },
  "dependencies": {
    "axios": "^1.3.6"
  },
  "devDependencies": {
    "electron": "^24.1.2",
    "electron-reload": "^2.0.0-alpha.1"
  }
}
